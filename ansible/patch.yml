- name: "Auto Patching Playbook"
  hosts: localhost
  connection: local
  gather_facts: true
  vars_files:
    - vars/main.yml

  roles:
    - pre_check
    - sand_reboot
    - stop_services
    - patch
    - reboot
