- name: "Auto Patching Playbook"
  gather_facts: true
  hosts: all
  become: true
  become_user: root
  become_method: sudo
  vars_files:
    - vars/main.yml

  roles:
    - pre_check
    - stop_services
    - sanity_reboot
    - stop_services
    # - patch
    # - reboot
